# Structure Elements
<br>

### 역할
주요 구조 부재인 기둥(Column), 슬래브(Slab), 보(Beam)를 모두 선택합니다.
<br>

### 분석
- **`categories: [/COLUMN|SLAB|BEAM/]`**: 정규표현식을 사용하여 IFC 엔티티 타입이 `IfcColumn`, `IfcSlab`, 또는 `IfcBeam`인 모든 객체를 검색 조건으로 설정합니다.
