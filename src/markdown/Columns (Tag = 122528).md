# Columns (Tag = 122528) 쿼리
<br>

### 역할
`Tag` 속성값이 정확히 "122528"인 모든 기둥(Column) 요소를 선택합니다.
<br>

### 분석
이 쿼리는 객체가 가진 고유 속성(Attribute) 값을 기준으로 필터링합니다.

1.  **`categories: [/COLUMN/]`**: 먼저 모든 `IfcColumn` 객체를 대상으로 합니다.
2.  **`attributes: { queries: [...] }`**: 객체의 속성을 검사합니다.
3.  **`{ name: /^Tag$/, value: /^122528$/ }`**: `Tag`라는 이름의 속성을 찾고, 그 값이 정확히 "122528"인 객체만 최종 결과로 선택합니다. (`^`와 `$`는 문자열의 시작과 끝을 의미하여 정확한 일치를 보장합니다.)